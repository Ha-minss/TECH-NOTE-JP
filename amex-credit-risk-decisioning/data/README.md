# Data

Raw AMEX data is not included.

Included sample data:

- `data/sample/synthetic_scores.csv`

The sample file is a synthetic test fixture used only for smoke tests. It is not derived from AMEX customers and must not be used for portfolio performance claims.

