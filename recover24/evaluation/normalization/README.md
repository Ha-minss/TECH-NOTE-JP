# Normalization Evaluation

This track checks two things deterministically:

- explicit user input -> canonical JSON
- canonical JSON -> document-facing display values

Current scope:
- KRW amount normalization
- date normalization
- unknown/empty handling
- required-field blocking
- document slot rendering checks
