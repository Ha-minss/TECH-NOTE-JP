# LLM Planner Prompt

Select allowed `data_need` values based on the parsed case and retrieved policy excerpts.

- Choose only from the provided allowlist.
- Do not pick tools directly.
- Do not diagnose the final cause.
- Return JSON only.
